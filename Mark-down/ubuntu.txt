wsl --install -d Ubuntu

Enter new UNIX username: dreams

Installing VS Code Server for x64 (f80445acd5a3dadef24aa209168452a3d97cc326)
Downloading:  62%

C:\Users\17026\Downloads\Learning\Computer Science

cd "Coding C++/C++ CS 202/Week 6/pa13"

scp 

scp -r a2001904928@bellagio.csn.edu:"~/" ~/

$ scp USERNAME@bellagio.csn.edu:"~/filename.txt" /home/vy/filename.txt

$ scp -r USERNAME@bellagio.csn.edu:"~/directory" /home/vy/

$ scp -r /home/vy/DIRECTORY_NAME USERNAME@bellagio.csn.edu:"~/"

sudo apt update

sudo apt install astyle

sudo apt install valgrind

Ensure Clang is installed#
Clang may already be installed on your Mac. 
To verify that it is, open a macOS Terminal window and enter the following command:

clang --version

If Clang isn't installed, enter the following command to install the command 
line developer tools:

xcode-select --install

mkdir projects

cd projects

mkdir helloworld

cd helloworld

code .

sudo apt install openjdk-11-jdk-headless  # version 11.0.13+8-0ubuntu1~20.04, or
sudo apt install default-jdk              # version 2:1.11-72
sudo apt install openjdk-16-jdk-headless  # version 16.0.1+9-1~20.04
sudo apt install openjdk-17-jdk-headless  # version 17.0.1+12-1~20.04
sudo apt install openjdk-8-jdk-headless   # version 8u312-b07-0ubuntu1~20.04
sudo apt install openjdk-13-jdk-headless  # version 13.0.7+5-0ubuntu1~20.04
sudo apt install ecj                      # version 3.16.0-1

valgrind --leak-check=full \
         --show-leak-kinds=all \
         --track-origins=yes \
         --verbose \
         --log-file=valgrind-out.txt \
         ./executable exampleParam1

sudo apt install valgrind  # Ubuntu, Debian, etc.
sudo yum install valgrind  # RHEL, CentOS, Fedora, etc.
sudo pacman -Syu valgrind  # Arch, Manjaro, Garuda, etc

sudo apt-get update

sudo apt install clang-9 --install-suggests

sudo apt install clang-12 --install-suggests

sudo apt install clang-11 --install-suggests

 sudo apt install clang